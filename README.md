# 后端启动

cd backend_cyl

## 启动后端服务
docker compose up -d --build


# 前端启动

cd frontend_cyl

## 启动前端服务
npm install
npm run dev

# 启动成功后，访问服务

http://localhost:5173/



# 配置

环境变量统一存放在backend_cyl/.env文件中，根据自身系统更改配置



